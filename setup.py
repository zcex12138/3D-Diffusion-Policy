from setuptools import setup, find_packages

setup(
    name='diffusion_policy_3d',
    version='0.0.0',
    packages=find_packages(),
    python_requires='>=3.8',
    install_requires=[
        # Add your dependencies here if needed
    ],
    author='Your Name',
    description='3D Diffusion Policy',
    long_description='A 3D diffusion policy implementation',
    long_description_content_type='text/markdown',
)
